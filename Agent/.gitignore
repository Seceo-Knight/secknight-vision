# Real config with your server's secrets - never commit this
config.json

# Saved login session (accessToken) - never commit this either
session.json

# Python
__pycache__/
*.pyc
.venv/
venv/

# PyInstaller
build/
dist/
*.spec.bak
